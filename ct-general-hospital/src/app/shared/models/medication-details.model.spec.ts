import { MedicationDetails } from './medication-details.model';

describe('MedicationDetails', () => {
  it('should create an instance', () => {
    expect(new MedicationDetails()).toBeTruthy();
  });
});
