import { ProcedureDetails } from './procedure-details.model';

describe('ProcedureDetails', () => {
  it('should create an instance', () => {
    expect(new ProcedureDetails()).toBeTruthy();
  });
});
