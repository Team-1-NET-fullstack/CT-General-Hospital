import { DiagnosisDetails } from './diagnosis-details.model';

describe('DiagnosisDetails', () => {
  it('should create an instance', () => {
    expect(new DiagnosisDetails()).toBeTruthy();
  });
});
